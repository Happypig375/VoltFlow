# Copy this file to .env.seed.local and set an absolute path.
# This is only for local seeding scripts (server-side), not for the browser app.
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/firebase-service-account.json # Downloaded from Google Firebase Console
